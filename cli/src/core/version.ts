// The plainkeep-core binary version, served by `plainkeep-core --version` / `--core-selftest`.
// 0.0.0 marks the Phase-1 skeleton: the binary exists and is identity-probeable, but the
// dispatcher/guardrail/resolver are not wired yet (Tasks 2–4). Bump once real verbs are dispatched.
export const CORE_VERSION = "0.0.0";
